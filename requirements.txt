# Pinned for reproducible runs. See pyproject.toml for the loose ranges used when
# installing the package itself. Regenerate with: pip freeze | grep -iE 'numpy|scikit|matplotlib'
numpy==2.2.6
scikit-learn==1.7.2
matplotlib==3.10.9
